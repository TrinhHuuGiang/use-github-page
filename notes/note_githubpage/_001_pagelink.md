By default, page locate at site with format: 
    `https://username.github.io/Repo-enable-githubpage/`

Note:
- only `README.md` at `root` can display as a html page. 
- others markdown have convert to html by some tools like: MkDocs, Jekyll
 